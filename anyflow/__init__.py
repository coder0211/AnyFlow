"""any-flow: MCP-served workflows that guide an AI agent step by step.

The core (`anyflow.core`) defines the interfaces every flow implements against;
flows (`anyflow.flows`) are the concrete, cloneable implementations; the server
(`anyflow.server`) exposes them over MCP.
"""

__version__ = "0.1.0"
